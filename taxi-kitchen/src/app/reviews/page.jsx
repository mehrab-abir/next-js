import React from 'react';
import Reviews from './Reviews';

export const metadata = {
    title : {
        absolute : "Satisfied users"
    }
}

const page = () => {
    return (
        <Reviews></Reviews>
    );
};

export default page;